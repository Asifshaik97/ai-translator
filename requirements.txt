Flask==3.0.3
deep-translator==1.11.4
langdetect==1.0.9
gTTS==2.5.1
pytesseract==0.3.13
Pillow==10.4.0
PyPDF2==3.0.1
python-docx==1.1.2
python-dotenv==1.0.1
gunicorn
# ---------------------------------------------------------------------------
# OPTIONAL — server-side speech-to-text with OpenAI Whisper.
# The app works fully without these (voice input uses the browser's built-in
# Web Speech API). Uncomment below only if you want a server-side /api/stt
# endpoint powered by Whisper (heavier install, needs ffmpeg on the system).
# ---------------------------------------------------------------------------
# openai-whisper==20231117
# ffmpeg-python==0.2.0
 
